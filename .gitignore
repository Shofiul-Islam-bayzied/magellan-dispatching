node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Runtime data — contains admin password and analytics IDs
data/

# Environment variables — contains secret credentials, never commit
.env
*.json.key